{
  "name": "paw-voyage-web",
  "description": "Paw Voyage - a bus-sort puzzle: a slice of a shipped Unity game ported to Phaser 4 + TypeScript",
  "version": "1.4.0",
  "license": "MIT",
  "licenseUrl": "http://www.opensource.org/licenses/mit-license.php",
  "scripts": {
    "dev": "vite --config vite/config.dev.mjs",
    "build": "vite build --config vite/config.prod.mjs",
    "check-core": "grep -rnE \"from .phaser.\" src/game/core/ && (echo CORE-LAYER-VIOLATION && exit 1) || echo \"core is clean: no phaser import\""
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.3.1"
  },
  "dependencies": {
    "phaser": "4.0.0",
    "terser": "^5.39.0"
  }
}
